import setuptools

setuptools.setup(
    name="challenge",
    version="0.0.1",
    description="Challenge for Loka's interview process",
    packages=setuptools.find_packages(),
    python_requires='>=3.7',
)
